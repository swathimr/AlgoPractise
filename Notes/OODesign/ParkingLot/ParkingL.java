package OODesign.ParkingLot;

public class ParkingL {

    // return the spot where vehicle was placed
    public void placeVehicle(Vehicle vehicle) {
        // 4 stacks. place vehicle can be done is O(1)
        // bascially depnding on type of vehicle size place them in one of these
        // Add to hashmap after placeing
    }
    public void removeVehicle(Vehicle vehicle) {
        // remove from hashmap and add it back to the stack
    }
}
